{
  "name": "azure-dyndns-updater",
  "version": "1.0.0",
  "description": "NodeJS script to update Azure DNS records with your current public IP address in the desired record.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@azure/arm-dns": "^5.1.0",
    "@azure/identity": "^3.1.2",
    "dotenv": "^16.0.3"
  }
}
